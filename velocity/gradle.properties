# FreeDraw Velocity plugin - forwards freedraw:payload plugin messages through the proxy.
# Keep the version in sync with the Fabric mod + Bukkit plugin.
plugin_version=1.1.0

# Java toolchain
java_version=21
